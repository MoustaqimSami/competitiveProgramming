#include<bits/stdc++.h>
using namespace std;

int findTarget(long long a[], int current) {
    int target = -1;
    if (current = 0) {
        target 
         = long long a[1]
    }
    long long strongest = a[0];
    return a[0];
}

int main() {
    long long A, B, C;
    cin >> A >> B >> C;

    long long strengths[] = {A, B, C};

    int i = 0;

    while(true) {
        int target = findTarget(strengths, i);
    }

}

